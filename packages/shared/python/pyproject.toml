[build-system]
requires = ["setuptools>=65.0"]
build-backend = "setuptools.build_meta"

[project]
name = "financeagent-shared"
version = "0.0.1"
description = "Shared Pydantic models for FinanceAGENT"
requires-python = ">=3.11"
dependencies = [
  "pydantic>=2.5"
]
